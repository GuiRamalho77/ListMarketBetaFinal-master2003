<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.listmarketbeta" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="16" />

    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Base.ThemeOverlay.AppCompat.ActionBar">
        <activity android:name="activity.LoginActivity"
            android:label="List Market"
           >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <!--  Entry for RegisterActivity.class -->
        <activity android:name="activity.RegisterActivity"
            android:label="List Market"></activity>
        <activity android:name="activity.FormularioProdutos"></activity>
        <activity android:name="activity.FormularioListasCompras"/>
        <activity android:name="activity.LitagemListasActivity"/>

    </application>

</manifest>
